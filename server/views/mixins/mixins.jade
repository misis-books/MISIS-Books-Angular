mixin js(url)
  script(src!=url, type='text/javascript')

mixin js_list(urls)
    each url in urls
      +js(url)

mixin css(url)
  link(href= url, type='text/css', rel='stylesheet')

mixin link(url, text)
  a(ng-href=url)&attributes(attributes)= text